# Backend API URL (defaults to http://localhost:8080 if not set)
# For Vercel deployment, add this variable in the Project Settings > Environment Variables
VITE_API_URL=https://your-backend-url.onrender.com
